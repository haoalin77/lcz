% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect_objects.R
\name{collect_objects}
\alias{collect_objects}
\title{收集当前环境中符合特定命名规则的对象}
\usage{
collect_objects(
  pattern,
  position = c("start", "end", "any"),
  envir = parent.frame()
)
}
\arguments{
\item{pattern}{字符型。用于匹配的关键词（例如 "data" 或 "model"）。}

\item{position}{字符型。指定匹配位置：
\itemize{
  \item \code{"start"}: 匹配以 pattern 开头的对象（默认）。
  \item \code{"end"}: 匹配以 pattern 结尾的对象。
  \item \code{"any"}: 只要名字中包含 pattern 即可。
}}

\item{envir}{环境。指定在哪个环境中搜索，默认为调用该函数的环境 (parent.frame)。}
}
\value{
一个命名列表 (Named List)，包含所有匹配到的对象。如果没有匹配到对象，返回空列表。
}
\description{
该函数用于查找当前环境中以指定字符串“开头”或“结尾”的所有对象，
并将它们的值收集到一个命名列表中返回。
}
\examples{
\dontrun{
# 1. 创建一些测试数据
data_2020 <- 1:10
data_2021 <- 11:20
model_lm <- "Linear Model"
result_lm <- "Result"
test_data <- "Test"

# 2. 获取所有以 "data" 开头的对象
list_start <- collect_objects("data", position = "start")
# 结果包含: data_2020, data_2021

# 3. 获取所有以 "lm" 结尾的对象
list_end <- collect_objects("lm", position = "end")
# 结果包含: model_lm, result_lm
}

}
