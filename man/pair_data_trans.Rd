% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pair_data_prepare.R
\name{pair_data_trans}
\alias{pair_data_trans}
\title{Reshape Data from Long to Wide with Grouped Variable Sorting}
\usage{
pair_data_trans(data, id_col = "site", group_col = "season", sep = "_")
}
\arguments{
\item{data}{A data frame. The input data in long format.}

\item{id_col}{Character string. The name of the column identifying unique subjects/rows
(e.g., "site"). Default is "site".}

\item{group_col}{Character string. The name of the column containing the grouping
variable to spread across columns (e.g., "season"). Default is "season".}

\item{sep}{Character string. The separator used between variable name and group level.
Default is "_".}
}
\value{
A data frame in wide format. The first column is the `id_col`, followed by
the reshaped data columns sorted by variable blocks.
}
\description{
This function takes a long-format data frame, pivots it to a wide format based on a
grouping column (e.g., season), and explicitly reorders the resulting columns.
It ensures that all split columns belonging to the same original variable are
placed adjacently (e.g., wt_1, wt_2, wt_3, wt_4), rather than grouping by the
time point.
}
\examples{
\dontrun{
# Create dummy data
data <- data.frame(
  site = rep(1:25, each = 4),
  season = rep(1:4, 25),
  wt = runif(100, 10, 30),
  ph = runif(100, 7, 9),
  do = runif(100, 5, 10)
)

# Run the function
result <- pair_data_trans(data, id_col = "site", group_col = "season")

# Check columns
colnames(result)
}

}
